name: spectra
channels:
  - defaults

dependencies:
  - python=3.11
  - numpy
  - scipy
  - pandas
  - matplotlib
  - scikit-learn
  - pip

  - pip:
      - plotly
      - polars
      - pytest
      - kaleido